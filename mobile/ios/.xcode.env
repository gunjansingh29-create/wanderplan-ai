# WanderPlan AI iOS – Xcode environment overrides
# Copy this file to .xcode.env.local and set real values.
# This file is safe to commit; .xcode.env.local should be gitignored.

# Path to the node binary used to start Metro bundler
export NODE_BINARY=$(command -v node)
